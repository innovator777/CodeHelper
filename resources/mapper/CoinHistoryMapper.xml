<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="codehelper.web.servlet.store.mapper.CoinHistoryMapper">
  <select id="selectByMember" parameterType="string" resultType="Report">
  	select
  	cId AS id,
  	cKind AS type,
  	mId AS memberId,
  	qId AS questionId
  	from coinhistory_tb
  </select>
  
  <insert id="insert" parameterType="CoinHistory" useGeneratedKeys="true" keyProperty="id">
		<selectKey keyProperty="id" resultType="int" order="BEFORE">
			select coinhistory_seq_seq.nextval from dual
		</selectKey>
		insert into coinhistory_tb(cId, cKind, mId, qId)
		values
		(#{id}, #{type}, #{memberId}, #{questionId}, #{questionId}, #{answerId})
	</insert>
  </mapper>